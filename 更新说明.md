# Fluent Melody 1.2.1

- 在主界面「转换与音轨」新增「AI 自动改编转换」开关，默认开启；无需进入设置或开启 Beta。
- 开启时使用本地旋律模型辅助编排；关闭时完全跳过模型，使用普通音轨与乐句规则。两种方式都保留单音、可演奏音域与至少 0.1 秒松键间隔。
- 开关即时保存，重启恢复；已载入歌曲时切换会停止单机播放并重新转换，不自动播放。转换过程中再次修改会按最后一次选择重新计算，过期结果不会覆盖新选择。
- 已有演奏版 NBS 继续优先保留原编排。该开关不控制 Beta 中的服务器 AI 编曲，也不修改房间正在使用的乐谱。

## 1.2.0

- 拖入 `.mid`、`.midi` 或 `.nbs` 文件，默认回到单机页面载入歌曲并自动转换，不会自动播放或上传。拖入多个文件时只载入第一个支持的文件；已在房间内需先退出再换歌。
- 歌曲拖放可经过日志、输入框等子控件；补充 Windows 管理员权限下的原生文件拖放处理。实际提升权限后从资源管理器拖入的情况仍需本机实测。
- 多人合奏、AI 编曲及其服务器连接设置默认隐藏，在「设置 → 实验功能 → Beta 模式」开启。开关即时保存，重启保留；旧版设置默认关闭。有房间或正在发出的连接、提交请求时，先完成相关操作再关闭。已提交的 AI 任务可以保留，关闭后暂停本机查询，重新开启后继续查询；服务端编曲不会因此取消。
- 新增「关于」页面，署名为「制作作者：性邓的小馒头」，说明 FluentPy 为自研的独立 MIT UI 库，并分别列明第三方依赖的许可来源。
- 附带的 FluentPy 增加导航入口显隐接口，沿用原有控件风格。开始／准备与暂停／继续快捷键继续使用本机设置，默认 F1 / F4。
- 转换启用低音、高音与半音的组合：左键＋中键为低音半音，右键＋中键为高音半音，左右键不会同按。支持完整 MIDI 48–85（C3 至 C♯6），在音域内尽量保留原音高，边缘半音不必折回普通音区。每次仍只按一个音符键，松键后至少等待 0.1 秒；旧歌曲需重新载入或转换以应用新规则。
- 默认自动转换加入真实离线旋律模型，采用 ISMIR 2019 论文作者公开的 POP CNN 权重，模型文件 874,991 字节，保留 MIT 许可。通过 NumPy 在本机推理，无需网络、API 或 token，不携带 PyTorch、TensorFlow 或 ONNX Runtime；模型来源与自研 FluentPy UI 库分别说明。
- 旋律选择结合音轨特征、乐句休止、重复同音和节拍取舍，区分 MIDI 实际按键时长与踏板延响。器乐前奏、间奏、尾奏也参与编排；密集片段允许最多 50 毫秒的局部起音微调，不累积漂移，必要时仍会减速或删减。
- 已编排且声部匹配的 NBS 优先保留现有编排；重新识别应载入原始 MIDI / NBS。模型不可用时明确提示并回退规则。离线识别与 Beta 中的服务器 AI 编曲独立，详见「本地旋律识别说明.md」。

新版发布目录独立，旧版 exe、已有服务器数据库和本机设备凭据不迁移、不覆盖。各版客户端使用同一个本机设置目录及实例锁，日常只运行其中一版。

## 1.1

设置页面新增可编辑快捷键：

- 开始／停止，在多人房间中作为准备键。
- 暂停／继续，房主可控制全房间。
- 点击输入框后按键录入，应用后保存；支持恢复默认 F1 / F4。
- 支持功能键及 Ctrl、Alt、Shift 组合。过滤演奏键、重复组合和系统保留键。
- 注册冲突时提示原因并恢复旧设置；录入期间暂停监听，结束录入时等待松键，避免误触。
- 播放按钮、准备按钮和帮助文字随设置同步变化。旧版设置会保留并自动补上默认快捷键。

界面继续沿用 FluentPy 的样式。
