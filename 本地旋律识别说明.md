# 本地旋律识别

主界面「转换与音轨」中的「AI 自动改编转换」默认开启。载入原始 MIDI / NBS 时，会先用随包附带的模型估计各音符的旋律显著性，再结合音轨、音区、乐句与节拍完成单音编排。它在本机运行，无需联网、注册、API 密钥或兑换 token。

关闭开关后不调用模型，使用普通音轨与乐句规则完成转换，单音、音域与松键间隔限制仍然保留。选择即时保存；切换后会停止当前单机演奏并重新转换歌曲，需要再次按开始键播放。转换中切换时只采用最后一次选择对应的结果。开关在主界面，不需要开启 Beta 模式。

## 模型来源

模型来自 Simonetta、Cancino-Chacón、Ntalampiras、Widmer 的 ISMIR 2019 论文 **A Convolutional Approach to Melody Line Identification in Symbolic Scores**，使用作者公开的 POP 训练权重。模型和来源仓库采用 MIT 许可，声明保留在 `licenses/Symbolic-Melody-Identification-MIT.txt`。

- [作者仓库](https://github.com/LIMUNIMI/Symbolic-Melody-Identification)
- [论文](https://github.com/LIMUNIMI/Symbolic-Melody-Identification/blob/master/paper.pdf)
- [作者公开的 POP 权重](https://github.com/LIMUNIMI/Symbolic-Melody-Identification/blob/master/nn_kernels_pop.pkl)
- [原许可](https://github.com/LIMUNIMI/Symbolic-Melody-Identification/blob/master/LICENSE)

这是第三方公开模型，不是本程序作者自行训练的模型，也没有用用户歌曲重新训练。程序将原有推理移植为 NumPy 实现，保留原权重数值；运行时读取安全数组文件并检查完整性，不加载 pickle。`symbolic_melody_pop.npz` 为 874,991 字节，完整来源、校验值和移植细节见源码中的 `assets/models/MODEL_CARD.md`。

本地推理不依赖 PyTorch、TensorFlow 或 ONNX Runtime，不会在启动或转换时下载模型。界面使用的 **FluentPy** 另为自研 MIT UI 库，与上述模型来源无关。

## 如何参与转换

模型分数只作为参考。程序还会分析音轨特征、乐句中的休止、重复同音、局部音区和节拍，避免每遇到伴奏起音就往主旋律中加音。无人声的前奏、间奏和尾奏按器乐段处理，不以人声模型的单音分数决定全部取舍。

MIDI 分别保留实际按键时长和踏板延响；识别旋律时使用实际按键时长，避免长时间延音踏板将输入变成持续和弦。普通 NBS 没有松键事件，只能根据音符间隔推定时长。

输出支持 MIDI 48–85（C3 至 C♯6），包括低音、高音与半音的组合。每位演奏者同时只按一个音符键，前一音松开后至少间隔 0.1 秒。为保留略密的旋律，单个起音可微调最多 50 毫秒，调整不逐音累积；无法满足间距的片段仍需减速或删减。

## 使用与限制

直接载入原始 MIDI / NBS 即可使用。已有本程序时长扩展且声部数匹配的演奏版 NBS，会优先保留原来的编排；要重新识别，请载入原始歌曲。文件缺失、校验失败或模型运行异常时，界面会说明原因，并使用音轨与乐句规则继续转换。

模型侧重符号乐谱中的人声旋律，器乐独奏、复杂琶音和特殊分轨可能出现漏选或误选。分数不是经过校准的正确概率；此前针对具体歌曲做出的演奏版也仅作为回归参考，不能当作人工标注标准谱或据此宣称准确率。建议先试听需要演奏的段落，必要时指定音轨。

本地旋律识别默认开启，不消耗服务器余额。设置中的 Beta 模式控制「多人合奏」和「AI 编曲」入口；后者是独立服务器服务，需要单独配置 API 或本机编曲提供方，并兑换其 token。
