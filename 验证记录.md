# 1.2.1 验证记录

2026-09-17，Windows / Python 3.13。使用模拟输入后端，未向游戏发送键鼠操作。

- 原有测试与新增核心测试共 132 项，在 Windows Qt 后端运行全部通过，无跳过；新增开关界面测试 12 项单独通过，共 144 项。
- 核心测试确认关闭时不导入、不调用旋律模型；默认及显式开启会调用模型；主动关闭与模型故障提示区分，普通转换仍满足单音、音域及松键间隔约束。
- 界面测试覆盖主界面位置、Beta 独立、默认与非法配置处理、持久化、写入失败回滚、停止播放与清除旧结果、转换中反复切换、过期错误丢弃、失败可重试、多人房间隔离及拖入时使用保存的选项。
- 源码客户端自检通过：本地模型实际参与转换、普通规则转换、主界面开关可见、关闭后保存并重新转换，以及快捷键、Beta、关于页面检查。
- 补充跨房间回归：本地转换未完成时进入房间、修改开关、再退出，旧转换无论成功或失败都不会覆盖最后一次选择。
- 1.2.1 打包客户端自检通过，并检查主界面开关开／关截图；离线模型、普通规则、保存及重新转换均在打包内容中运行验证。采用一次性普通权限副本，核对 Python 归档不变；正式 EXE 保留管理员权限声明。
- 新版两种服务器 EXE 使用临时数据库通过启动、token、房间准备及暂停联调，未调用付费 AI。完整发布目录约 190.4 MB，压缩包与解压总量都受 200,000,000 字节上限检查。

## 历史：1.2 验证记录

2026-09-17，Windows / Python 3.13。所有演奏测试使用模拟输入后端，没有向游戏发送键鼠操作。

## 本次自动检查

- 全套 126 项测试：123 项通过；无窗口的 Qt 后端跳过 3 项 Windows 原生拖放检查。随后切换真实 Windows Qt 后端，拖放组 10 项全部通过，补齐这 3 项。
- 新增覆盖拖入主窗口／日志／输入框、中文 HDROP、跨通道去重、关闭清理、Beta 默认关闭与持久化、进行中请求保护、AI 暂停查询与恢复、关于署名及许可文件。
- 全音域 MIDI 48～85、左键＋中键／右键＋中键、自然音等价按法、NBS 回读、按键顺序、暂停／停止／异常后的完整松键通过；松键间隔仍至少 0.1 秒。
- 旋律选择覆盖主句休止、前奏／间奏／尾奏、低音旋律、高位伴奏、低置信度回退、重复音与密集段、50 毫秒内局部起音修正及不累积漂移。
- 真实 MIDI 踏板与速度变化测试确认物理松键时长和踏板延响分别保留，转换不修改原始音符数据。
- 第三方 CNN 的 NumPy 推理与作者公开 Theano 参考矩阵核对，最大差约 1.28e-5；安全 NPZ 校验、并发、无效输入和踏板处理通过。客户端无按键自检确认本地模型实际参与转换，没有使用规则回退冒充模型成功。

## 真实歌曲回归

使用用户提供的 FF14《稻香》《花海》、NBS《千本樱》、MIDI《天空之城》《诀别书》比较旧选择逻辑与新版本，检查来源轨道、完整时间线、旋律休止、密集段和松键间隔。源文件 SHA-256 均未改变。单曲本地识别和转换在开发电脑上约 1～2 秒；这不是所有电脑的速度保证。

旧专项演奏版仅作回归参考，并非人工标注的标准答案。新版减少了多首歌曲中伴奏抢占主旋律的情况，但复杂段仍可能漏选、误选或因演奏速度限制删音；不能据此声称所有曲目都优于人工改编。

## 1.2 发布包检查

- 实际运行两个服务端 EXE，临时数据库下完成 token 发行、两名签名客户端兑换、上传 NBS、加入房间、统一准备倒计时和房主暂停；独立 AI 服务的启动与额度查询通过，未调用付费提供方。
- 打包后的客户端实际读取并转换示例；另用普通多轨 NBS 确认离线 CNN 真实参与转换，避免资源遗漏或依赖缺失被回退逻辑掩盖。
- 打包客户端内检查快捷键保存、Beta 默认隐藏及开关保存、关于作者与 FluentPy 说明；已检查单机、设置、Beta 和关于页截图。
- 客户端测试仍使用一次性普通权限副本，核对 Python 归档不变；正式交付 EXE 保留管理员权限声明，未自动确认 UAC。
- 完整发布目录约 190.4 MB，客户端、两种服务端功能、离线模型及运行依赖均包含在内；打包脚本同时限制 ZIP 和解压文件总和不得超过 200,000,000 字节。

## 实际环境边界

已验证 Windows 原生文件消息的解析与处理；尚未实际从 Explorer 拖入提升权限的正式 EXE。游戏中半音组合、键鼠接受情况、跨电脑合奏听感仍需实际试奏。发布包附带《半音组合试音.nbs》。没有调用外部付费模型或向服务上传用户歌曲。

## 历史：1.1 验证记录

2026-09-14，Windows / Python 3.13。

## 自动测试

`python -m unittest discover -s tests -v`：**67 项通过**。

| 范围 | 数量 | 覆盖内容 |
| --- | ---: | --- |
| 音乐核心 | 9 | MIDI 变速与延音、NBS 回读、旋律/前奏、多人编排、音符校验和试听 |
| 演奏时序 | 10 | 单线程输入、按键释放、0.1 秒间隔、暂停和未来时间续播、异常清理 |
| 联机与鉴权 | 8 | 兑换与绑定、自然时间到期、请求签名和重放防护、统一准备与掉线暂停 |
| 客户端联调 | 8 | 两名签名客户端、实际房间状态接入 Qt、过时响应、断线恢复和本机输入故障 |
| AI 服务 | 10 | 模拟提供方、真实 NBS 输出、权限、幂等、额度与退款、重启恢复、输入和输出限制 |
| FluentPy | 2 | 已销毁控件的主题切换、导航展开宽度 |
| 快捷键后端 | 12 | 按键解析与限制、注册冲突回退、暂停录入、松键重触发和线程退出 |
| 快捷键界面 | 8 | 旧设置兼容、录入与取消、保存与默认值、按钮提示同步、防误触和失败回退 |

另外读取原有青花瓷、稻香、千本樱和诀别书文件，检查了 1、3、8 人的编排结果，源文件未修改。

额外使用 Windows 原生接口，验证了空闲组合键的注册、暂停、改绑、恢复及释放；此检查没有模拟键盘或鼠标输入。

## 发布包检查

- 实际运行 `FluentMelodyServer.exe` 两个独立进程，分别启动联机和 AI 服务，使用临时数据库。
- 实际使用服务端 exe 发行 token，两个签名客户端兑换、加入房间、上传双人 NBS、全部准备、统一倒计时并暂停。
- AI 服务 exe 的启动、兑换及独立额度查询通过；未调用外部付费模型。
- 客户端打包内容启动、FluentPy 图标加载、《两只老虎》读取与转换、NBS 导出及回读通过。
- 在客户端打包内容中进入设置，将快捷键改为 F8 和 Ctrl+Alt+P，检查设置保存及按钮提示同步，并检查设置页截图。
- 客户端检查使用一次性副本，将该副本的 Windows 权限声明改为普通权限，保留并逐字节核对同一份 Python 归档及依赖。交付的正式 exe 仍保留管理员权限声明，未自动操作 UAC 授权。
- 打包时限定依赖搜索目录，避免收集到 PDF 工具的同名 ICU 库；FluentPy 资源显式包含在发布包中。
- 1.1 发布目录独立为 `release-1.1.0`，保留原 `release` 目录及其中可能存在的服务器数据。

## 尚需实际环境验证

所有自动演奏测试均禁用真实键鼠输入。没有在游戏中试奏，没有完成跨电脑公网的合奏听感测试，也没有使用真实 API 密钥或 Codex 账户生成编曲。AI 的额度、协议和错误处理通过受控模拟验证；实际编曲质量取决于运营者配置的模型。

程序不会承诺提示词绝不被模型复述。API 密钥不进入提示上下文，用户只收到经校验的音乐文件；请求量、并发、时长和输出规模有明确限制。本机 Codex 仍需按说明放在独立服务账户中运行。
